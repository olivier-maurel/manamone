<form method="post" action="{{ path('app_row_virtual_delete', {'id': row_virtual.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ row_virtual.id) }}">
    <button class="btn">Delete</button>
</form>
